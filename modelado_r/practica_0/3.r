#Ejercicio 3. Definiendo y operando con vectores
#Definir en R los vectores w3 = c(-2, 3, 2, 0) y w4 = c(4, 9, 4, 0), predecir los resultados de las
#siguientes operaciones y usar R para verificarlas:
w3 = c(-2, 3, 2, 0)
w4 = c(4, 9, 4, 0)
w3-w4; #resta
w4-w3; #resta
w4*w3; #multiplacion de cada componente
w4^w3; # potencia de cada componente
w4/w3; # division de cada componente
(1:4)^2; #loop de 1 a 4 al cuadrado
(1:4)^(0:3); #loop de 1 a 4 con loop de potencias de 0 a 3 
(1:2)*(0:3); #loop de 1 a 4 con loop de multiplicaciones de 0 a 3